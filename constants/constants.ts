export const LS_ACCESS_TOKEN = 'pstoreAccessToken';

export const statsKeys = {
  usersCount: 'Total Users',
  tokensCount: 'Total Tokens',
};

export const DEFAULT_LIMIT = 9;
export const TABLET_LIMIT = 8;
